PORT=3000

# SMTP settings (example for Google Workspace / Gmail SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@domain.com
SMTP_PASS=your-app-password-or-smtp-password
SMTP_FROM=DDP Labs <your-email@domain.com>
